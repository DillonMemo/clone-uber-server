type Verification {
  id: Int!
  target: String!
  payload: String!
  key: String!
  verified: Boolean!
  createdAt: String!
  updatedAt: String
}

# Example
# v = Verification
# v.target = 'phone'
# v.payload = +821055496100
# v.key = 3456

# result => 'hello verift with the number 3456'
